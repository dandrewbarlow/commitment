var elements;
shiftVal = document.getElementsByClassName("body").style.left;

while (1) {
  var left = 0;
  var delta = 1;
  elements = left;
  if (left >= 100 | left <= 0) {
    delta = -delta;
  }
  left += delta;
}
